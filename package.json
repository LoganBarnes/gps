{
  "name": "friends",
  "version": "0.0.0",
  "homepage": "https://loganbarnes.github.io/gps",
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.11"
  },
  "dependencies": {
    "babylonjs": "^6.38.0"
  }
}
